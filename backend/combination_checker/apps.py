from django.apps import AppConfig


class CombinationCheckerConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "combination_checker"
    verbose_name = "Проверка комбинаций препаратов"
